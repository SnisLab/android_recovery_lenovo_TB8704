#!/sbin/sh

LOG=/tmp/recovery.log

# QSEE expects the legacy bootdevice alias; fstab keeps direct physical paths.
rm -Rf /dev/block/bootdevice >> "$LOG"
ln -vs /dev/block/platform/soc/7824900.sdhci /dev/block/bootdevice >> "$LOG"
